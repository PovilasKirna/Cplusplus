#include <iostream>
#include <fstream>
#include <vector>
using namespace std;

vector<vector<int>> S;
vector<int>
vector<vector<int>> B;
vector<vector<int>> V;
vector<vector<int>> K;

void read();
int main() {
  read();
  return 0;
}

void read(){
  char card;
  int number;
  ifstream fin ("/Users/Povilas/Documents/GitHub/Cplusplus/Tourney_kortos/input.txt");
  while(!fin.eof()){
    fin>>card;
    fin>>number;
    if(card == 'S'){

    }
  }
}